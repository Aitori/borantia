import React from 'react';
import styled from 'styled-components'

const NavWrapper = styled.div`
  display: flex;
  color: #F69AF866;
`

const Navbar = () => {
  return <NavWrapper>Borantio</NavWrapper>
}

export default Navbar;
